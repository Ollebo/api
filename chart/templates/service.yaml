apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: ollebo-api
spec:
  type: ClusterIP
  sessionAffinity: None
  selector:
    app: ollebo-api
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
