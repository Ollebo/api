__pycache__

# Local env (may hold overrides/secrets); keep .env.example as the template.
.env

# Legacy on-disk snapshot kept for reference only; do not ship.
code_old/

# Local Meilisearch runtime data from previous stack — Meili is no longer used.
meili_data/

# Local test payloads for callback smoke tests; not for shipping.
code/db/testData/
